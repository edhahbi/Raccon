#pragma once

#include <stdbool.h>
#include <pci/types.h>

#define Min(x,y) (x)>(y)?(y):(x);

#define ip_addr_t const char*
#define u8 uint8_t
#define u16 uint16_t
#define u32 uint32_t
#define u64 uint64_t
